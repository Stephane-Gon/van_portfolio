export const AVAIALABLE_ENTENSIONS = ['.webp', '.svg'];
export const FILE_DISPLAY_NAME_MAX_LENGTH = 30;
export const FILE_SIZE_LIMIT = 1 * 1024 * 1024; // 1 MB in bytes

export type TOGGLE_TABS = 'list' | 'detail';

export enum SkillTypes {
  frontend = 'frontend',
  backend = 'backend',
  testing = 'testing',
  uiUx = 'ui_ux',
  ciCd = 'ci_cd',
}
